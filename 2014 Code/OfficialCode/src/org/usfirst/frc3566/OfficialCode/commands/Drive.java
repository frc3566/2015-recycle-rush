// RobotBuilder Version: 1.0
//
// This file was generated by RobotBuilder. It contains sections of
// code that are automatically generated and assigned by robotbuilder.
// These sections will be updated in the future when you export to
// Java from RobotBuilder. Do not put any code or make any change in
// the blocks indicating autogenerated code or it will be lost on an
// update. Deleting the comments indicating the section will prevent
// it from being updated in the future.
package org.usfirst.frc3566.OfficialCode.commands;
import edu.wpi.first.wpilibj.command.Command;
import org.usfirst.frc3566.OfficialCode.Robot;
import org.usfirst.frc3566.OfficialCode.RobotConstants;
/**
 * Drive forward for a specified time or distance (or both).
 */
public class Drive extends Command {
    private double ySpeed = 1.0; // whoa, Nelly!
    private double xSpeed = 0.0;
    private double distanceInFeet = -1;
    private double initialDistanceInInches;
    /**
     * Start driving forward. At full speed.
     */
    public Drive() {
        // BEGIN AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=REQUIRES
        requires(Robot.driveTrain);
    // END AUTOGENERATED CODE, SOURCE=ROBOTBUILDER ID=REQUIRES
    }
    /**
     * Start driving forward at a particular speed.
     *
     * @param percentageOfFullSpeed 0-1
     */
    public Drive(double percentageOfFullSpeed) {
        ySpeed = percentageOfFullSpeed;
        requires(Robot.driveTrain);
    }
    /**
     * Drive forward at a particular speed for a particular distance.
     *
     * @param percentageOfFullSpeed 0-1
     * @param distanceInFeet
     */
    public Drive(double percentageOfFullSpeed, double distanceInFeet) {
        ySpeed = percentageOfFullSpeed;
        this.distanceInFeet = distanceInFeet;
        requires(Robot.driveTrain);
    }
    //For Auton ONLY
    public Drive(double xPercentageOfFullSpeed, double yPercentageOfFullSpeed, double distanceInFeet) {
        ySpeed = yPercentageOfFullSpeed;
        xSpeed = -xPercentageOfFullSpeed; // so that the left front encoder will be turned by positive nunmbers
        this.distanceInFeet = distanceInFeet;
        requires(Robot.driveTrain);
    }
    // Called just before this Command runs the first time
    protected void initialize() {
        this.setTimeout(0.8);
        initialDistanceInInches = Robot.driveTrain.getDistance();
    }
    // Called repeatedly when this Command is scheduled to run
    protected void execute() {
        if (this.isTimedOut()) {
            Robot.driveTrain.cartesian(xSpeed, ySpeed, 0, 0);
        } else {
            Robot.driveTrain.cartesian(xSpeed, ySpeed, 0, 0);
        }
        if (distanceInFeet > 0) {
            if ((Robot.driveTrain.getDistance() - initialDistanceInInches) >= (distanceInFeet * 12.0/*inches*/)) {
                Robot.driveTrain.cartesian(xSpeed, ySpeed, 0, 0);
            }
        }
    }
    // Make this return true when this Command no longer needs to run execute()
    protected boolean isFinished() {
        if (distanceInFeet > 0) {
            return (Robot.driveTrain.getDistance() - initialDistanceInInches) >= (distanceInFeet * 12.0/*inches*/);
        } else {
            return false;
        }
    }
    // Called once after isFinished returns true
    protected void end() {
        Robot.driveTrain.stop();
    }
    // Called when another command which requires one or more of the same
    // subsystems is scheduled to run
    protected void interrupted() {
        end();
    }
}
